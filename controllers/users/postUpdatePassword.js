const asyncHandler = require("../../middlewares/async");
const data = require("../../data");

const users = data.users;
const validators = data.validators;

exports.postUpdatePasswordController = asyncHandler(async (req, res, next) => {});
